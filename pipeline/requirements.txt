# Tiny Earth data pipeline — Python dependencies.
# All ship manylinux wheels, so `pip install -r requirements.txt` needs no system libs.

numpy>=1.26        # arrays, the cube-sphere grids
scipy>=1.11        # ETOPO NetCDF reader (scipy.io)
netCDF4>=1.6       # ETOPO NetCDF reader (fallback)
shapely>=2.1.2       # land/lake polygon geometry + point-in-polygon
pyshp>=2.3         # read Natural Earth shapefiles (pure Python, no GDAL)
rasterio>=1.3      # read the Köppen-Geiger GeoTIFF
Pillow>=10.0       # write the inner-globe texture PNG
PyYAML>=6.0        # planet.yaml config
requests>=2.31     # download source datasets

# dev / tests
pytest>=7.4
pytest-cov>=4.1
